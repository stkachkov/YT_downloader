# Это пример конфигурационного файла для yt-dlp.
# Чтобы использовать его, переименуйте этот файл в 'yt-dlp.conf' и поместите
# в одну из директорий, где yt-dlp ищет конфигурацию.
# Для Linux это обычно ~/.config/yt-dlp/yt-dlp.conf
# Для Windows это C:\Users\<ВашеИмя>\AppData\Roaming\yt-dlp\yt-dlp.conf

# --- Путь для сохранения ---
# Раскомментируйте и измените путь, чтобы сохранять видео в определенную папку.
# Например, в папку "Загрузки" вашего домашнего каталога.
# -P "~/Downloads"

# --- Шаблон для имени файла ---
# Создает "красивые" имена файлов.
# Формат: "Название видео - ID видео.расширение"
-o "%(title)s - %(id)s.%(ext)s"

# --- Выбор качества ---
# Скачивать лучшее доступное видео с аудио.
# Для видео с разрешением до 1080p это обычно работает отлично.
-f "bestvideo[ext=mp4]+bestaudio[ext=m4a]/best[ext=mp4]/best"

# --- Субтитры ---
# Раскомментируйте, если хотите автоматически скачивать субтитры.
# --write-auto-subs
# --sub-langs "en,ru" # Предпочитаемые языки субтитров (английский, русский)

# --- Другие полезные опции ---
# Встраивать метаданные в видеофайл.
--embed-metadata

# Не перезаписывать существующие файлы.
--no-overwrites
